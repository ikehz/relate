%h1= link_to @contact.name, @contact

.pull-right= link_to 'Edit Contact', edit_contact_path(@contact), class: 'btn btn-default'

= render 'conversations/table'

= raw render_markdown(@contact.notes)
